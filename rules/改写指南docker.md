# 改写指南：
### 将ipynb文件内容改写成一个python文件代码，并存储到新文件中
### 要求：
#### 1.支持gpu/MPS/cpu训练，启动时根据系统判断并决定那种训练方式
#### 2.模型代码文件支持在win、mac、linux本地和docker中进行训练，保存模型，并支持单次预测以及支持启动http服务支持单次预测请求
##### a.当使用docker进行操作，支持docker compose和docker原生启动方式
##### b.模型保存应该在进行单次预测时使用起来更加合理
##### c.支持启动http服务支持单次预测请求应当新增加代码文件提供http服务，与模型训练/预测文件进行分离
#### 3.当本机是MPS时，不使用docker的方式
#### 4.输出相关运行的命令文件:使用README.md